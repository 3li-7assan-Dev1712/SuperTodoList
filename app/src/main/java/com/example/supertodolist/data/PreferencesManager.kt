package com.example.supertodolist.data


class PreferencesManager/* @Inject constructor(private val sharedPreferences: SharedPreferences)*/{
/*

    fun  setSortOrder(sortOrder: SortOrder){
        when (sortOrder){
            SortOrder.BY_NAME -> {
                sharedPreferences.edit().putString("sort_order_by_name", "sort_by_name").apply()
            }
            SortOrder.BY_DATE -> {
                sharedPreferences.edit().putString("sort_order_by_date", "sort_by_date").apply()
            }
        }
    }

    fun setHideCompletedTask(hideCompletedTasks: Boolean){
        when (hideCompletedTasks){
            true -> {
                sharedPreferences.edit().putBoolean("hide_completed", true).apply()
            }
            else -> {
                sharedPreferences.edit().putBoolean("hide_completed", false).apply()
            }
        }
    }*/

}